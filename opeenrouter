---
title: Quickstart
subtitle: Get started with OpenRouter
slug: quickstart
headline: OpenRouter Quickstart Guide | Developer Documentation
canonical-url: 'https://openrouter.ai/docs/quickstart'
'og:site_name': OpenRouter Documentation
'og:title': OpenRouter Quickstart Guide
'og:description': >-
  Get started with OpenRouter's unified API for hundreds of AI models. Learn how
  to integrate using OpenAI SDK, direct API calls, or third-party frameworks.
'og:image':
  type: url
  value: >-
    https://openrouter.ai/dynamic-og?pathname=quickstart&title=Quick%20Start&description=Start%20using%20OpenRouter%20API%20in%20minutes%20with%20any%20SDK
'og:image:width': 1200
'og:image:height': 630
'twitter:card': summary_large_image
'twitter:site': '@OpenRouterAI'
noindex: false
nofollow: false
---

OpenRouter provides a unified API that gives you access to hundreds of AI models through a single endpoint, while automatically handling fallbacks and selecting the most cost-effective options. Get started with just a few lines of code using your preferred SDK or framework.

<Tip>
  Looking for information about free models and rate limits? Please see the [FAQ](/docs/faq#how-are-rate-limits-calculated)
</Tip>

In the examples below, the OpenRouter-specific headers are optional. Setting them allows your app to appear on the OpenRouter leaderboards.

## Using the OpenAI SDK

<CodeGroup>

```python title="Python"
from openai import OpenAI

client = OpenAI(
  base_url="https://openrouter.ai/api/v1",
  api_key="<OPENROUTER_API_KEY>",
)

completion = client.chat.completions.create(
  extra_headers={
    "HTTP-Referer": "<YOUR_SITE_URL>", # Optional. Site URL for rankings on openrouter.ai.
    "X-Title": "<YOUR_SITE_NAME>", # Optional. Site title for rankings on openrouter.ai.
  },
  model="openai/gpt-4o",
  messages=[
    {
      "role": "user",
      "content": "What is the meaning of life?"
    }
  ]
)

print(completion.choices[0].message.content)
```

```typescript title="TypeScript"
import OpenAI from 'openai';

const openai = new OpenAI({
  baseURL: 'https://openrouter.ai/api/v1',
  apiKey: '<OPENROUTER_API_KEY>',
  defaultHeaders: {
    'HTTP-Referer': '<YOUR_SITE_URL>', // Optional. Site URL for rankings on openrouter.ai.
    'X-Title': '<YOUR_SITE_NAME>', // Optional. Site title for rankings on openrouter.ai.
  },
});

async function main() {
  const completion = await openai.chat.completions.create({
    model: 'openai/gpt-4o',
    messages: [
      {
        role: 'user',
        content: 'What is the meaning of life?',
      },
    ],
  });

  console.log(completion.choices[0].message);
}

main();
```

</CodeGroup>

## Using the OpenRouter API directly

<Tip>
  You can use the interactive [Request Builder](/request-builder) to generate OpenRouter API requests in the language of your choice.
</Tip>

<CodeGroup>

```python title="Python"
import requests
import json

response = requests.post(
  url="https://openrouter.ai/api/v1/chat/completions",
  headers={
    "Authorization": "Bearer <OPENROUTER_API_KEY>",
    "HTTP-Referer": "<YOUR_SITE_URL>", # Optional. Site URL for rankings on openrouter.ai.
    "X-Title": "<YOUR_SITE_NAME>", # Optional. Site title for rankings on openrouter.ai.
  },
  data=json.dumps({
    "model": "openai/gpt-4o", # Optional
    "messages": [
      {
        "role": "user",
        "content": "What is the meaning of life?"
      }
    ]
  })
)
```

```typescript title="TypeScript"
fetch('https://openrouter.ai/api/v1/chat/completions', {
  method: 'POST',
  headers: {
    Authorization: 'Bearer <OPENROUTER_API_KEY>',
    'HTTP-Referer': '<YOUR_SITE_URL>', // Optional. Site URL for rankings on openrouter.ai.
    'X-Title': '<YOUR_SITE_NAME>', // Optional. Site title for rankings on openrouter.ai.
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    model: 'openai/gpt-4o',
    messages: [
      {
        role: 'user',
        content: 'What is the meaning of life?',
      },
    ],
  }),
});
```

```shell title="Shell"
curl https://openrouter.ai/api/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $OPENROUTER_API_KEY" \
  -d '{
  "model": "openai/gpt-4o",
  "messages": [
    {
      "role": "user",
      "content": "What is the meaning of life?"
    }
  ]
}'
```

</CodeGroup>

The API also supports [streaming](/docs/api-reference/streaming).

## Using third-party SDKs

For information about using third-party SDKs and frameworks with OpenRouter, please [see our frameworks documentation.](/docs/community/frameworks)

快速入门


复制页面

OpenRouter 入门

OpenRouter 提供了一个统一的 API，让您可以通过单个终端节点访问数百个 AI 模型，同时自动处理回退并选择最具成本效益的选项。使用您首选的 SDK 或框架，只需几行代码即可开始使用。

正在寻找有关免费模型和速率限制的信息？请参阅常见问题解答

在下面的示例中，特定于 OpenRouter 的标头是可选的。设置它们将允许您的应用程序显示在 OpenRouter 排行榜上。

使用 OpenAI SDK

蟒

TypeScript （类型脚本）

from openai import OpenAI
客户端 = OpenAI （
 base_url=“https://openrouter.ai/api/v1”，
 api_key=“<OPENROUTER_API_KEY>”，
)
completion = 客户端。聊天。完成。创建 （
extra_headers={
 “HTTP-Referer”： “<YOUR_SITE_URL>”， # 可选。openrouter.ai 上排名的站点 URL。
 “x-title”： “<YOUR_SITE_NAME>”， # 可选。openrouter.ai 排名的网站名称。
},
 model=“openai/gpt-4o”，
消息=[
    {
 “role”： “用户”，
 “content”： “生命的意义是什么？“
    }
  ]
)
print（completion.choices[0] 的 Choices[0] 中。消息。内容）
直接使用 OpenRouter API
您可以使用交互式 Request Builder 以您选择的语言生成 OpenRouter API 请求。


蟒

TypeScript （类型脚本）

壳

导入请求
导入 JSON
响应 = 请求。post（
 url=“https://openrouter.ai/api/v1/chat/completions”，
头={
 “Authorization”： “不记名<OPENROUTER_API_KEY>”，
 “HTTP-Referer”： “<YOUR_SITE_URL>”， # 可选。openrouter.ai 上排名的站点 URL。
 “x-title”： “<YOUR_SITE_NAME>”， # 可选。openrouter.ai 排名的网站名称。
},
 data=json 中。转储（{
 “model”： “openai/gpt-4o”， # 可选
 “消息”： [
      {
 “role”： “用户”，
 “content”： “生命的意义是什么？“
      }
    ]
  })
)
该 API 还支持流式处理。

使用第三方 SDK
有关将第三方 SDK 和框架与 OpenRouter 结合使用的信息，请参阅我们的框架文档。

API 参考


复制页面

OpenRouter 的 API 概述

OpenRouter 的请求和响应架构与 OpenAI Chat API 非常相似，但有一些细微的差异。在高层次上，OpenRouter 跨模型和提供程序规范化架构，因此您只需要学习一个。

请求
完成请求格式
下面是作为 TypeScript 类型的请求架构。这将是您向终端节点发出的请求的正文（有关示例，请参阅上面的快速入门）。POST/api/v1/chat/completions

有关参数的完整列表，请参阅参数。

请求架构

亚型的定义如下
类型 请求 = {
“messages” 或 “prompt” 为必填项
 messages？： 消息[];
 prompt？： 字符串;
如果未指定 “model”，则使用用户的默认值
 model？： 字符串; 请参阅“支持的型号”部分
允许强制模型生成特定的输出格式。
请参阅模型页面并在此文档页面上说明哪些模型支持它。
 response_format？： { type： 'json_object' };
 stop？： 字符串 | 字符串[];
 stream？： 布尔值; //启用流式处理
请参阅 LLM 参数 （openrouter.ai/docs/api-reference/parameters）
 max_tokens？： 数字; 范围 ： [1， context_length）
 temperature？： 数字; 范围 ： [0， 2]
工具调用
将按原样传递给实现 OpenAI 接口的提供商。
对于具有自定义接口的提供程序，我们转换和映射属性。
否则，我们将工具转换为 YAML 模板。模型使用助手消息进行响应。
请参阅支持工具调用的模型：openrouter.ai/models?supported_parameters=tools
 tools？： 工具[];
 tool_choice？： ToolChoice;
高级可选参数
 seed？： 数字; 仅限整数
 top_p？： 数字; 范围 // （0， 1]
 top_k？： 数字; 范围：[1， Infinity） 不适用于 OpenAI 模型
 frequency_penalty？： 数字; 范围 ： [-2， 2]
 presence_penalty？： 数字; 范围 ： [-2， 2]
 repetition_penalty？： 数字; 范围 ： （0， 2]
 logit_bias？： { [key： number]： 数字 };
 top_logprobs：数字; 仅限整数
 min_p？： 数字; 范围 ： [0， 1]
 top_a？： 数字; 范围 ： [0， 1]
通过为模型提供预测输出来减少延迟
https://platform.openai.com/docs/guides/latency-optimization#use-predicted-outputs
 prediction？： { type： '内容'; 内容： 字符串 };
仅限 OpenRouter 的参数
请参阅“Prompt Transforms”部分：openrouter.ai/docs/transforms
 transforms？： string[];
请参阅“模型布线”部分：openrouter.ai/docs/model-routing
 models？： string[];
 route？： 'fallback';
请参阅“提供程序路由”部分：openrouter.ai/docs/provider-routing
 provider？： ProviderPreferences;
 user？： 字符串;为您的最终用户提供稳定的标识符。用于帮助检测和防止滥用行为。
};
亚：
键入 TextContent = {
 type： 'text';
 text： 字符串;
};
键入 ImageContentPart = {
 type： 'image_url';
 image_url： {
 url： 字符串; URL 或 base64 编码的图像数据
 detail？： 字符串; 可选，默认为 “auto”
  };
};
键入 ContentPart = TextContent | ImageContentPart 的
类型 消息 =
| {
 角色： 'user' | “助理” | 'system' 的;
ContentParts 仅适用于 “user” 角色：
 内容：字符串 | 内容部分[];
如果包含 “name”，则其前缀如下
对于非 OpenAI 模型： '{name}： {content}'
 name？： 字符串;
    }
| {
 role： '工具';
 内容：字符串;
 tool_call_id：字符串;
 name？： 字符串;
    };
类型 FunctionDescription = {
 description？： 字符串;
 名称：字符串;
 参数： object; JSON Schema 对象
};
类型 工具 = {
 type： 'function';
 function： 函数描述;
};
类型 ToolChoice =
 | '无'
 | '自动'
| {
 type： 'function';
 函数： {
 名称：字符串;
      };
    };
该参数可确保您从 LLM 收到结构化响应。该参数仅受 OpenAI 模型、Nitro 模型和其他一些模型支持 - openrouter.ai/models 上模型页面上的提供程序检查它是否受支持，并在您的 Provider Preferences （提供程序首选项） 中设置为 true。请参阅 Provider Routingresponse_formatrequire_parameters

头
OpenRouter 允许您指定一些可选的标头来识别您的应用程序并使其可供我们网站上的用户发现。

HTTP-Referer：在 openrouter.ai 上识别您的应用
X-Title：设置/修改应用的标题
TypeScript （类型脚本）

fetch（'https://openrouter.ai/api/v1/chat/completions'， {
 method： 'POST'，
 标头： {
 授权：“Bearer <OPENROUTER_API_KEY>”，
 'HTTP-Referer'： '<YOUR_SITE_URL>'， // 可选。openrouter.ai 上排名的站点 URL。
 'X-title'： '<YOUR_SITE_NAME>'， // 可选。openrouter.ai 排名的网站名称。
 '内容类型'： 'application/json'，
  },
 body： JSON的 JSON 中。stringify（{
 型号： 'OpenAI/GPT-4O'，
 消息：[
      {
 role： 'user'，
 内容：“生命的意义是什么？'，
      },
    ],
  }),
});
模型路由
如果省略该参数，则使用用户或付款人的默认值。 否则，请记住从支持的 models 或 API 中，并包含组织 前缀。OpenRouter 将选择最便宜和最好的 GPU，用于 处理请求，如果它收到 5xx 响应代码，或者如果您受到速率限制。modelmodel

流
服务器发送的事件 （上交所），以便为所有模型启用流式处理。只需发送您的请求正文即可。SSE 流偶尔会包含 一个 “comment” 有效负载，您应该忽略它（如下所述）。stream: true

非标准参数
如果所选模型不支持请求参数（例如在非 OpenAI 模型中，或对于 OpenAI），则忽略该参数。 其余的将转发到底层模型 API。logit_biastop_k

助理预填充
OpenRouter 支持要求模型完成部分响应。这对于指导模型以某种方式响应非常有用。

要使用此功能，只需在数组末尾包含一条消息 with 即可。role: "assistant"messages

TypeScript （类型脚本）

fetch（'https://openrouter.ai/api/v1/chat/completions'， {
 method： 'POST'，
 标头： {
 授权：“Bearer <OPENROUTER_API_KEY>”，
 '内容类型'： 'application/json'，
  },
 body： JSON的 JSON 中。stringify（{
 型号： 'OpenAI/GPT-4O'，
 消息：[
 { role： '用户'， content： '生命的意义是什么？' }，
 { role： 'assistant'， content： “我不确定，但我最好的猜测是” }，
    ],
  }),
});
反应
Completions响应格式
OpenRouter 跨模型和提供程序规范化架构，以符合OpenAI 聊天 API.

这意味着它始终是一个数组，即使模型只返回一个完成。如果请求了流，则每个选项都将包含一个属性，否则将包含一个属性。这样可以更轻松地对所有模型使用相同的代码。choicesdeltamessage

以下是作为 TypeScript 类型的响应架构：

TypeScript （类型脚本）

亚型的定义如下
类型 响应 = {
 id： 字符串;
根据您是否将 “stream” 设置为 “true” 以及
无论您传入的是 “messages” 还是 “prompt”，您
将获得不同的输出形状
 选项：（NonStreamingChoice | 流选择 | NonChatChoice）[];
 created： number; Unix 时间戳
 型号：字符串;
 对象： 'chat.completion' | 'chat.completion.chunk';
 system_fingerprint？： 字符串; 仅在提供程序支持时存在
始终为非流式处理返回使用情况数据。
流式传输时，您将在
end 伴随着一个空 choices 数组。
 usage？： ResponseUsage;
};
如果提供程序返回 usage，我们会将其向下传递
原样。否则，我们使用 GPT-4 分词器进行计数。
类型 ResponseUsage = {
/** 包括图像和工具（如果有） */
 prompt_tokens：数字;
/** 生成的令牌 */
 completion_tokens：数字;
/** 上述两个字段的总和 */
 total_tokens：数字;
};

亚：
类型 NonChatChoice = {
 finish_reason：字符串 | null;
 text： 字符串;
 error？： ErrorResponse;
};
类型 NonStreamingChoice = {
 finish_reason：字符串 | null;
 native_finish_reason： 字符串 | null;
 消息：{
 内容：字符串 | null;
 角色：字符串;
 tool_calls？： ToolCall[];
  };
 error？： ErrorResponse;
};
类型 StreamingChoice = {
 finish_reason：字符串 | null;
 native_finish_reason： 字符串 | null;
 增量： {
 内容：字符串 | null;
 role？： 字符串;
 tool_calls？： ToolCall[];
  };
 error？： ErrorResponse;
};
类型 ErrorResponse = {
 代码：数字; 请参阅“错误处理”部分
 消息：字符串;
 metadata？： Record<string， unknown>; 包含其他错误信息，例如提供程序详细信息、原始错误消息等。
};
键入 ToolCall = {
 id： 字符串;
 type： 'function';
 function： FunctionCall;
};

下面是一个示例：

{
 “id”： “gen-xxxxxxxxxxxxxx”， /
 “选择”： [
    {
 “finish_reason”： “stop”， // 标准化finish_reason
 “native_finish_reason”： “stop”， // 来自提供者的原始finish_reason
 “消息”： {
如果流式传输，则为 “delta”
 “role”： “助理”，
 “content”： “你好！“
      }
    }
  ],
 “用法”： {
 “prompt_tokens”：0、
 “completion_tokens”：4、
 “total_tokens”：4
  },
 “model”： “openai/gpt-3.5-turbo” // 也可以是 “anthropic/claude-2.1” 等，具体取决于最终使用的 “model”
}

完成原因
OpenRouter 将每个模型的 normalize 为以下值之一：、、、.finish_reasontool_callsstoplengthcontent_filtererror

某些模型和提供商可能还有其他完成原因。模型返回的原始 finish_reason 字符串可通过属性获得。native_finish_reason

查询 Cost 和 Stats
completions API 响应中返回的令牌计数不会通过模型的原生分词器进行计数。相反，它使用标准化的、与模型无关的计数（通过 GPT4o 分词器完成）。这是因为某些提供程序无法可靠地返回本机令牌计数。但是，这种行为变得越来越罕见，我们将来可能会向响应对象添加本机令牌计数。

积分使用和模型定价基于原生令牌计数（而不是 API 响应中返回的“标准化”令牌计数）。

要使用模型的原生分词器进行精确的令牌核算，您可以通过终端节点检索完整的生成信息。/api/v1/generation

请求完成后，您可以使用 returned to 查询生成统计信息（包括令牌计数和成本）。这就是获取所有模型和请求（流式处理和非流式处理）的成本和令牌的方法。id

查询生成统计信息

const 生成 = await fetch（
 'https://openrouter.ai/api/v1/generation?id=$GENERATION_ID'，
 { 标头 }、
);
const stats = 等待生成。json（）的;
有关完整的响应形状，请参阅生成 API 参考。

请注意，对于非流式完成，响应正文的字段中也提供了令牌计数。usage

OpenRouter API 允许来自任何模型的流式处理响应。这对于构建聊天界面或其他应用程序非常有用，其中 UI 应在模型生成响应时更新。

要启用流式处理，您可以在请求中将参数设置为 to。然后，该模型会将响应以块的形式流式传输给客户端，而不是一次返回整个响应。streamtrue

以下是如何流式传输响应并对其进行处理的示例：


蟒

TypeScript （类型脚本）

import requests
import json
question = "How would you build the tallest building ever?"
url = "https://openrouter.ai/api/v1/chat/completions"
headers = {
  "Authorization": f"Bearer <OPENROUTER_API_KEY>",
  "Content-Type": "application/json"
}
payload = {
  "model": "openai/gpt-4o",
  "messages": [{"role": "user", "content": question}],
  "stream": True
}
buffer = ""
with requests.post(url, headers=headers, json=payload, stream=True) as r:
  for chunk in r.iter_content(chunk_size=1024, decode_unicode=True):
    buffer += chunk
    while True:
      try:
        # Find the next complete SSE line
        line_end = buffer.find('\n')
        if line_end == -1:
          break
        line = buffer[:line_end].strip()
        buffer = buffer[line_end + 1:]
        if line.startswith('data: '):
          data = line[6:]
          if data == '[DONE]':
            break
          try:
            data_obj = json.loads(data)
            content = data_obj["choices"][0]["delta"].get("content")
            if content:
              print(content, end="", flush=True)
          except json.JSONDecodeError:
            pass
      except Exception:
        break
其他信息
对于 SSE（服务器发送事件）流，OpenRouter 偶尔会发送注释以防止连接超时。这些注释如下所示：

: OPENROUTER PROCESSING

Comment payload 可以根据SSE 规格.但是，您可以根据需要利用它来改进用户体验，例如通过显示动态加载指示器。

某些 SSE 客户端实现可能不会根据规范解析有效负载，这会导致在非 JSON 有效负载时出现未捕获的错误。我们推荐以下客户：JSON.stringify

eventsource-解析器
OpenAI 开发工具包
Vercel AI 开发工具包
流取消
可以通过中止连接来取消流式处理请求。对于支持的提供商，这会立即停止模型处理和计费。

提供商支持
要实现流取消：


蟒

TypeScript （类型脚本）

import requests
from threading import Event, Thread
def stream_with_cancellation(prompt: str, cancel_event: Event):
    with requests.Session() as session:
        response = session.post(
            "https://openrouter.ai/api/v1/chat/completions",
            headers={"Authorization": f"Bearer <OPENROUTER_API_KEY>"},
            json={"model": "openai/gpt-4o", "messages": [{"role": "user", "content": prompt}], "stream": True},
            stream=True
        )
        try:
            for line in response.iter_lines():
                if cancel_event.is_set():
                    response.close()
                    return
                if line:
                    print(line.decode(), end="", flush=True)
        finally:
            response.close()
# Example usage:
cancel_event = Event()
stream_thread = Thread(target=lambda: stream_with_cancellation("Write a story", cancel_event))
stream_thread.start()
# To cancel the stream:
cancel_event.set()
取消仅适用于使用受支持提供商的流式处理请求。为 非流式处理请求或不支持的提供程序，模型将继续 处理，您将需要为完整的响应付费。

速率限制

创建额外的账户或 API 密钥不会影响您的速率限制，因为我们 全局治理容量。但是，我们对 不同的模型，因此如果您遇到 问题。

速率限制和剩余积分
要检查 API 密钥上剩余的速率限制或积分，请向 .https://openrouter.ai/api/v1/auth/key


TypeScript （类型脚本）

蟒

const response = await fetch('https://openrouter.ai/api/v1/auth/key', {
  method: 'GET',
  headers: {
    Authorization: 'Bearer <OPENROUTER_API_KEY>',
  },
});
如果您提交了有效的 API 密钥，您应该会收到以下形式的响应：

TypeScript （类型脚本）

type Key = {
  data: {
    label: string;
    usage: number; // Number of credits used
    limit: number | null; // Credit limit for the key, or null if unlimited
    is_free_tier: boolean; // Whether the user has paid for credits before
  };
};
无论账户状态如何，都有一些速率限制适用于某些类型的请求：

免费使用限制：如果您使用的是免费模型变体（ID 以 结尾），则每分钟最多可以发出 20 个请求。以下每日限制适用：:free
如果您购买的积分少于 10 个，则每天只能请求 50 个模型。:free

如果您购买了至少 10 个积分，则您的每日限制将增加到每天 1000 个模型请求。:free

DDoS 保护：Cloudflare 的 DDoS 保护将阻止大幅超出合理使用的请求。
如果您的账户的积分余额为负数，您可能会看到错误，包括免费型号的错误。添加积分以使您的余额高于零，这样您就可以再次使用这些模型。402

认证


复制页面

API 身份验证

您可以使用 OpenRouter API 密钥支付模型成本。

我们的 API 使用 Bearer 令牌对请求进行身份验证。这允许您使用 或curlOpenAI 开发工具包直接使用 OpenRouter。

OpenRouter 上的 API 密钥比直接用于模型 API 的密钥更强大。

它们允许用户为应用程序设置信用额度，并且可以在 OAuth 流中使用。

使用 API 密钥
要使用 API 密钥，请先创建您的密钥。为其命名，您可以选择设置信用额度。

如果您直接调用 OpenRouter API，请使用 API 密钥将标头设置为不记名令牌。Authorization

如果您使用的是 OpenAI Typescript SDK，请将 to 和 to 设置为 API 密钥。api_basehttps://openrouter.ai/api/v1apiKey


TypeScript （持有者令牌）

TypeScript （OpenAI 开发工具包）

蟒

壳

fetch('https://openrouter.ai/api/v1/chat/completions', {
  method: 'POST',
  headers: {
    Authorization: 'Bearer <OPENROUTER_API_KEY>',
    'HTTP-Referer': '<YOUR_SITE_URL>', // Optional. Site URL for rankings on openrouter.ai.
    'X-Title': '<YOUR_SITE_NAME>', // Optional. Site title for rankings on openrouter.ai.
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    model: 'openai/gpt-4o',
    messages: [
      {
        role: 'user',
        content: 'What is the meaning of life?',
      },
    ],
  }),
});
要使用 Python 进行流式传输，请参阅 OpenAI 的此示例.

如果您的密钥已泄露
您必须保护您的 API 密钥，切勿将其提交到公共存储库。

OpenRouter 是 GitHub 秘密扫描合作伙伴，并且有其他方法可以检测暴露的密钥。如果我们确定您的密钥已泄露，您将收到一封电子邮件通知。

如果您收到此类通知或怀疑您的密钥已泄露，请立即访问您的密钥设置页面以删除泄露的密钥并创建一个新密钥。

强烈建议使用环境变量并将键排除在代码库之外。
import OpenAI from 'openai';

const openai = new OpenAI({
  baseURL: 'https://openrouter.ai/api/v1',
  apiKey: '<OPENROUTER_API_KEY>',
  defaultHeaders: {
    'HTTP-Referer': '<YOUR_SITE_URL>', // Optional. Site URL for rankings on openrouter.ai.
    'X-Title': '<YOUR_SITE_NAME>', // Optional. Site title for rankings on openrouter.ai.
  },
});

async function main() {
  const completion = await openai.chat.completions.create({
    model: 'openai/gpt-4o',
    messages: [{ role: 'user', content: 'Say this is a test' }],
  });

  console.log(completion.choices[0].message);
}

main();

参数


复制页面

采样参数决定了模型的 token 生成过程。您可以将以下列表中的任何参数以及其他参数发送到 OpenRouter。

如果您的请求中没有某些参数（例如，1.0），OpenRouter 将默认为下面列出的值。我们还会将一些特定于提供程序的参数（例如 Mistral 或 Hyperbolic 的参数）直接传输到相应的提供程序（如果指定）。temperaturesafe_promptraw_mode

请参阅模型的 provider 部分以确认支持哪些参数。有关管理提供商特定参数的详细指导，请单击此处。

温度
钥匙：temperature
可选，浮点数，0.0 到 2.0 - 默认值：1.0
此设置会影响模型响应的多样性。较低的值会导致更可预测和典型的响应，而较高的值会鼓励更多样化和不太常见的响应。值为 0 时，模型始终为给定输入提供相同的响应。

解说视频：观看

前 P
钥匙：top_p
可选，浮点数，0.0 到 1.0 - 默认值：1.0
此设置将模型的选择限制为一定百分比的可能标记：仅限制概率加起来为 P 的前几个标记。较低的值使模型的响应更具可预测性，而默认设置允许选择各种标记。把它想象成一个充满活力的 Top-K。

解说视频：观看

Top K
钥匙：top_k
可选、整数、0 或以上 - 默认值：0
这限制了模型在每个步骤中选择的标记，使其从较小的集合中进行选择。值为 1 表示模型将始终选择最可能的下一个标记，从而获得可预测的结果。默认情况下，此设置处于禁用状态，使模型考虑所有选项。

解说视频：观看

频率损失
钥匙：frequency_penalty
可选，浮点型，-2.0 到 2.0 - 默认值：0.0
此设置旨在根据令牌在 input 中出现的频率来控制令牌的重复。它尝试降低使用那些在 input 中出现较多的标记的频率，这与它们出现的频率成正比。令牌惩罚随出现次数而变化。负值将鼓励令牌重用。

解说视频：观看

存在惩罚
钥匙：presence_penalty
可选，浮点型，-2.0 到 2.0 - 默认值：0.0
调整模型重复输入中已使用的特定标记的频率。较高的值使此类重复的可能性较小，而负值则相反。令牌惩罚不随出现次数而变化。负值将鼓励令牌重用。

解说视频：观看

重复罚分
钥匙：repetition_penalty
可选，浮点数，0.0 到 2.0 - 默认值：1.0
有助于减少输入中标记的重复。较高的值使模型不太可能重复标记，但值过高会使输出的连贯性降低（通常使用缺少小单词的连续句子）。Token 惩罚根据原始 Token 的概率进行缩放。

解说视频：观看

最小 P
钥匙：min_p
可选，浮点数，0.0 到 1.0 - 默认值：0.0
表示令牌的最小概率 相对于最可能的 Token 的概率。（该值根据最可能的令牌的置信度而变化。如果您的 Min-P 设置为 0.1，这意味着它只允许至少 1/10 的代币作为最佳选项。

顶部 A
钥匙：top_a
可选，浮点数，0.0 到 1.0 - 默认值：0.0
根据最可能标记的概率，仅考虑具有“足够高”概率的前 Token。把它想象成一个充满活力的 Top-P。较低的 Top-A 值将基于最高概率的标记进行选择，但范围较窄。较高的 Top-A 值不一定会影响输出的创造性，而是根据最大概率优化筛选过程。

种子
钥匙：seed
可选，整数
如果指定，则推理将确定性地进行采样，以便具有相同种子和参数的重复请求应返回相同的结果。某些模型不保证确定性。

最大令牌数
钥匙：max_tokens
可选，整数，1 或以上
这将设置模型可以生成的 Token 数的上限。它不会产生超过此限制的量。最大值是上下文长度减去提示长度。

Logit 偏置
钥匙：logit_bias
可选，映射
接受一个 JSON 对象，该对象将令牌（由其在 tokenizer 中的令牌 ID 指定）映射到从 -100 到 100 的关联偏差值。从数学上讲，偏差在采样之前被添加到模型生成的 logit 中。确切的效果因模型而异，但 -1 和 1 之间的值应降低或增加选择的可能性;像 -100 或 100 这样的值应该导致禁止或独占选择相关令牌。

日志
钥匙：logprobs
可选，布尔值
是否返回输出 Token 的对数概率。如果为 true，则返回返回的每个输出标记的对数概率。

Top Logprobs
钥匙：top_logprobs
可选，整数
一个介于 0 和 20 之间的整数，指定在每个标记位置最有可能返回的标记数，每个标记都有一个关联的对数概率。如果使用此参数，则必须将 logprobs 设置为 true。

响应格式
钥匙：response_format
可选，映射
强制模型生成特定的输出格式。设置为 '{ “type”： “json_object” }' 将启用 JSON 模式，这保证模型生成的消息是有效的 JSON。 **注意**：使用 JSON 模式时，您还应该通过系统或用户消息指示模型自己生成 JSON。

结构化输出
Key: structured_outputs
Optional, boolean
If the model can return structured outputs using response_format json_schema.

Stop
Key: stop
Optional, array
Stop generation immediately if the model encounter any token specified in the stop array.

工具
钥匙：tools
可选，数组
工具调用参数，遵循 OpenAI 的工具调用请求形状。对于非 OpenAI 提供商，它将进行相应的转换。[单击此处了解有关工具调用的更多信息]（/docs/requests#工具调用）

工具选择
钥匙：tool_choice
可选，数组
控制模型调用哪个 （如果有） 工具。'none' 表示模型不会调用任何工具，而是生成一条消息。'auto' 表示模型可以在生成消息或调用一个或多个工具之间进行选择。'required' 表示模型必须调用一个或多个工具。通过 '{“type”： “function”， “function”： {“name”： “my_function”}}' 指定特定工具会强制模型调用该工具。

对于错误，OpenRouter 返回具有以下形状的 JSON 响应：

type ErrorResponse = {
  error: {
    code: number;
    message: string;
    metadata?: Record<string, unknown>;
  };
};

在以下情况下，HTTP 响应将具有与 相同的状态代码，从而形成请求错误：error.code

您原来的请求无效
您的 API 密钥/账户积分不足
否则，返回的 HTTP 响应状态将为 LLM 生成输出时发生的任何错误都将在响应正文中发出或作为 SSE 数据事件发出。200

JavaScript 中打印错误的示例代码：

const request = await fetch('https://openrouter.ai/...');
console.log(request.status); // Will be an error code unless the model started processing your request
const response = await request.json();
console.error(response.error?.status); // Will be an error code
console.error(response.error?.message);

错误代码
400：错误请求（无效或缺少参数、CORS）
401：凭据无效（OAuth 会话过期、API 密钥已禁用/无效）
402：您的账户或 API Key 积分不足。添加更多积分，然后重试请求。
403：您选择的模型需要审核，并且您的意见已被标记
408：您的请求超时
429：您的速率受到限制
502：您选择的模型已关闭，或者我们收到了来自该模型的无效响应
503：没有满足路由要求的可用模型提供程序
审核错误
如果您的输入被标记，则 将包含有关问题的信息。元数据的形状如下：error.metadata

type ModerationErrorMetadata = {
  reasons: string[]; // Why your input was flagged
  flagged_input: string; // The text segment that was flagged, limited to 100 characters. If the flagged input is longer than 100 characters, it will be truncated in the middle and replaced with ...
  provider_name: string; // The name of the provider that requested moderation
  model_slug: string;
};

提供程序错误
如果模型提供程序遇到错误，则 将包含有关问题的信息。元数据的形状如下：error.metadata

type ProviderErrorMetadata = {
  provider_name: string; // The name of the provider that encountered the error
  raw: unknown; // The raw error from the provider
};

未生成任何内容时
有时，模型可能不会生成任何内容。这通常在以下情况下发生：

模型正在从冷启动中预热
系统正在扩展以处理更多请求
预热时间通常从几秒钟到几分钟不等，具体取决于型号和提供商。

如果您遇到持续的无内容问题，请考虑实施简单的重试机制，或者使用具有最近活动的其他提供程序或模型重试。

此外，请注意，在某些情况下，即使没有生成任何内容，上游提供商仍可能向您收取提示处理费用。